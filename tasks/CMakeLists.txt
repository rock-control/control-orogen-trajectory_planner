# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(trajectory_plannerTaskLib)
ADD_LIBRARY(${TRAJECTORY_PLANNER_TASKLIB_NAME} SHARED 
    ${TRAJECTORY_PLANNER_TASKLIB_SOURCES})
add_dependencies(${TRAJECTORY_PLANNER_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${TRAJECTORY_PLANNER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${TRAJECTORY_PLANNER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${TRAJECTORY_PLANNER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${TRAJECTORY_PLANNER_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${TRAJECTORY_PLANNER_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${TRAJECTORY_PLANNER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${TRAJECTORY_PLANNER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${TRAJECTORY_PLANNER_TASKLIB_HEADERS}
    DESTINATION include/orocos/trajectory_planner)

